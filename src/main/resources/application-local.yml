spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:vector_db}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:pgPasswd}
    driver-class-name: org.postgresql.Driver
